더블 버퍼링
	GUI나 모바일쪽 용어
		싱글 버퍼링으로 그래픽을 그릴 경우 데이터를 저장하는 동안에는
		다음 그림의 데이터를 전송할 수 없기 때문에 지우고 그리고를 반복할 때
		필연적으로 발생하는 깜빡임, 찢어짐을 막기 위해 사용되는 기법
		예비 버퍼(버퍼2)에 그림을 보내고 그것을 버퍼1이 처리하는 동안
		버퍼2의 데이터를 보내면 전송과 처리가 막히는 일이 없어진다.
		삭제 후 만드는 것이 아니라 교체의 느낌

리플렉션
	컴파일러가 번역을 하고 나서 메모리에 올라가야 하는데 
	【런타임 중에도 동적으로 필요한걸 가지고 올 수 있는 것】

DB 조회 방식 2가지
	풀테이블스캔방식
		데이터가 저장된 목록 중 【모든 데이터 요소를 차례차례 조사하여
		원하는 것을 찾아내는 것】
		튜플의 수가 많아지면 검색 시간이 매우 오래걸린다.
	
	인덱스스캔방식
		데이터의 주소를 기억하고 관리하는 인덱스 파일과
		실제 데이터를 기억하는 데이터 파일로 구성되어 있다.
		【인덱스 파일에서 찾고자하는 데이터가 저장된 주소를 찾고
		그 뒤 찾은 주소값을 통해 데이터 파일에서 데이터를 찾는다.】

오라클의 인덱스
	【순서가 없는 것에 순서를 붙여주는 것】, 잘 사용하면 정렬까지 가능하다.

Object Oriented Language
	객체 지향 언어

서블릿
	【클라이언트의 요청을 처리하고 결과를 웹 서버로 응답】해주는 서블릿 클래스의 구현 규칙을 지킨 자바 프로그램
	응답까지 한다. 
	응답 객체를 가지고 있는건 서블릿이 직접 관여하지 않지만 
	연산 결과를 담기위해 가지고 있는것
		응답 자체는 웹 서버에서 한다. 응답을 하기위한 준비

배열과 클래스 차이
	【배열은 같은 타입만 가능, 클래스는 다른 타입도 가능】

【큐 구조 : FIFO】
【스택 구조 : LIFO】

자바스크립트 콜백함수
	파라미터로 함수를 전달하는 함수
	자바스크립트 모듈에 있는 메소드를 사용하기 위해 리턴값을 매개변수로 담아서 보내줄 수 있다.

동적 바인딩
	【프로그램 실행 중】에 자료형과 함수의 타입이 결정되는 것
	타입이 정해져 있지 않기 때문에 정적 바인딩에 비해 실행 속도가 느림

static이 붙어 있으면 무조건 정적 바인딩
	재정의 절대 못함

메인메소드를 실행해주는 것은 JVM

무결성 제약 조건
	정확성, 일관성

서블릿 실행 과정
	요청이 왔을 때 요청을 요청 객체로 받아서 연산 후 
	응답 객체에 담아서 웹 서버에 전달하면
	디스트로이 메소드가 실행되서 요청과 응답 객체가 메모리에서 해제된다.

자바 특징
	보안성이 좋다
	멀티쓰레드 지원
	객체지향언어(생산성이 좋다)
	플랫폼에 독립적

백엔드
	보이지 않는 연산 등을 개발

변수나 이런걸 메모리에 할당하는 것은 JVM?

MVC패턴 쓰는 이유는 각각의 영역을 구분해서 결합도를 느슨하게 하기 위해

10개의 데이터를 전달할 때 어떤 컬렉션을 쓰냐?
	해쉬맵이 가장 좋음 
	key, value가 있어서

아우터조인은 언제 쓰는지?
	왼쪽에 있는건 전부 가져와야하고 오른쪽은 조건에 맞는 것만 가져와야 할 때

mybatis가 가지고 있는 2가지
	DBCP, JNDI

JNDI
	【xml에 있는 객체를 찾아서 자바로 가져오는 것】

ajax를 동기식으로 하는 이유
	속도가 너무 빠를 경우 연산이 누락되는 경우가 발생하여
	경우에 따라 동기식으로 변경한다.


JSON의 단점은 스키마가 없어서 데이터를 따로관리하기 위해서는 별도의 스키마가 필요하기 때문에 한계가 있다.

서블릿이 있는데 jsp를 쓰는 이유
	자바에서 html쓰는 것보다 html에서 자바를 쓰는게 더 편해서
