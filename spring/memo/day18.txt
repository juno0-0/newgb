if(조건식1 || 조건식2){ }
	조건식1이 참이면 조건식2는 검사도 안한다.★★★
	왠만하면 null 먼저 검사하고 size를 검사하자

showImage(path.replace(new RegExp(/\\/g), "/"));
	'\\'를 '/'로 바꾸는 정규식
	언제 써야할 지 모르겠다면 그냥 전부 다 바꿔서 사용하자
	
(1) 어떤 것이 프로세스에서 사용중인 지 모를 때
	가비지 컬렉터 : 주인이 없는 필드를 직접 찾아서 메모리에서 해제해주는 역할
	System.gc();
		가비지 컬렉터 호출 - 찾아도 바로 메모리 해제를 하지 않고 【세대를 붙여준다.】
		추후 【세대별 메모리 해제는 일괄 처리된다.】
		그 후 3세대까지 모이면 한번에 메모리 해제 이런식으로 되는 것
	System.runFinalization(); 
		세대별 상관없이 필드 메모리 해제(finalization() 호출), 자주쓰면 안좋다.
		세대가 모이지 않아도 메모리 해제

(2)정확히 닫아야 할 객체를 알고 있을 때
finally {
	try {
		in.close();
	} catch (Exception e) {
		e.printStackTrace();
	}
}

(function(){})();
	선언과 동시에 사용, 일회성	

Ajax 통신일 경우 대부분 RestController?

